{% extends 'admin/base-admin.html.twig' %}

{% block title %}Patient Details{% endblock %}

{% block body %}
<div class="container-fluid">
    <h1 class="mb-4">🧑‍⚕️ Patient Details</h1>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <table class="table table-bordered table-striped align-middle mb-0">
                <tbody>
                    <tr>
                        <th style="width: 200px;">ID</th>
                        <td>{{ patient.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ patient.name }}</td>
                    </tr>
                    <tr>
                        <th>Email</th>
                        <td>{{ patient.email }}</td>
                    </tr>
                    <tr>
                        <th>Address</th>
                        <td>{{ patient.address }}</td>
                    </tr>
                    <tr>
                        <th>Contact</th>
                        <td>{{ patient.contact }}</td>
                    </tr>
                    <tr>
                        <th>Gender</th>
                        <td>{{ patient.gender }}</td>
                    </tr>
                    <tr>
                        <th>Age</th>
                        <td>{{ patient.age }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="mt-4 d-flex gap-2">
        <a href="{{ path('app_patient_index') }}" class="btn btn-secondary">
            ⬅️ Back to List
        </a>

        <a href="{{ path('app_patient_edit', {'id': patient.id}) }}" class="btn btn-warning text-white">
            ✏️ Edit
        </a>

        {{ include('patient/_delete_form.html.twig', { 'button_class': 'btn btn-danger', 'button_label': '🗑️ Delete' }) }}
    </div>
</div>
{% endblock %}
