{% extends 'admin/base-admin.html.twig' %}

{% block title %}Add New Doctor{% endblock %}

{% block body %}
<div class="container-fluid">
    <h1 class="mb-4">➕ Add New Doctor</h1>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            {{ include('doctor/_form.html.twig', { button_label: 'Save Doctor' }) }}
        </div>
    </div>

    <div class="mt-3">
        <a href="{{ path('app_doctor_index') }}" class="btn btn-secondary">
            ⬅️ Back to List
        </a>
    </div>
</div>
{% endblock %}
