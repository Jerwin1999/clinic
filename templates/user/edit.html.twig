{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="container-fluid">
    <h1 class="mb-4 text-primary">Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="mt-3 d-flex gap-2">
        <a href="{{ path('app_user_index') }}" class="btn btn-secondary px-4 py-2 rounded-3">
            <i class="bi bi-arrow-left me-1"></i> Back to List
        </a>

        {{ include('user/_delete_form.html.twig', { 'class': 'd-inline' }) }}
        {# You can style the delete form button inside _delete_form.html.twig as below: 
            <button class="btn btn-danger px-4 py-2 rounded-3">
                <i class="bi bi-trash me-1"></i> Delete
            </button>
        #}
    </div>
</div>
{% endblock %}
