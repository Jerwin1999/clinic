{% extends 'admin/base-admin.html.twig' %}

{% block title %}User Index{% endblock %}

{% block body %}
<div class="container-fluid">
    <h1 class="mb-4">🧍‍♂️ User Management</h1>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h5 class="card-title mb-0">User List</h5>
                <a href="{{ path('app_user_new') }}" class="btn btn-primary btn-sm">
                    ➕ Create New User
                </a>
            </div>

            <table id="userTable" class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Username</th>
                        <th>Roles</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for user in users %}
                    <tr>
                        <td>{{ user.id }}</td>
                        <td>{{ user.username }}</td>
                        <td>
                            {% for role in user.roles %}
                                <span class="badge bg-info text-dark">{{ role|replace({'ROLE_':''}) }}</span>
                            {% endfor %}
                        </td>
                        <td class="text-center">
                            <a href="{{ path('app_user_show', {'id': user.id}) }}" class="btn btn-sm btn-info text-white me-1">
                                👁️ View
                            </a>
                            <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="btn btn-sm btn-warning text-white">
                                ✏️ Edit
                            </a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="text-center text-muted">No users found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

{% endblock %}
